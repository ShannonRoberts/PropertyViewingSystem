import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#00185D',
      light: '#3366FF'
    },
    secondary: {
      main: '#d1dbe8'
    },
    text: {
      primary: '#00185D',
      secondary: '#00185D',
    },
  },
});

export default theme;
